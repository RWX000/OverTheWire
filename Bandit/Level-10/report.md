# Objetive 
- The password for the next level is stored in the file data.txt, which contains base64 encoded data

## Solution
- For this level, I had to research what base64 is after learning that it's a way to encode images or text to binary, or vice versa. I used the command `base64 -d` to view the data.txt file that contained the password.
- dtR173fZKb0RRsDFSGsg2RWnpNVj3qRr
## Commans used 
- ssh 
- pwd
- ls
- cat 
- base64 -d data.txt
